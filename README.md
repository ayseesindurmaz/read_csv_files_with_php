# read_csv_files_with_php
That project reads .csv file which is selected from HTML file selecter. And then creates mysql tables which contains columns with csv file' columns and rows with csv file' rows.
